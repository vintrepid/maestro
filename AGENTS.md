# Agent Rules

1. Read `MAP.md` for the codebase architecture. Do NOT explore files manually.
   confirm: You have read MAP.md (state the module count)

2. Read and follow ALL rules in `rules.json`.
   confirm: You have read rules.json (state the count)

3. Read `current_task.json` for your current task and context from the previous session.
   confirm: You have read current_task.json (summarize the current state and what's next)
