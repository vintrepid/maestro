# Agent Rules

1. Read and follow ALL rules in `rules.json` (universal bundle — compact, right-sized).
   confirm:
     You have read rules.json (state the count)
     You will follow the rules

2. Read `current_task.json` for your current task and context from the previous session.
   confirm:
     You have read current_task.json (summarize the current state and what's next)

## Specialized Bundles

If your task is UI-focused, also read `rules.ui.md`.
If your task is model/backend-focused, also read `rules.model.md`.

Bundles are generated by `mix maestro.rules.export`.
