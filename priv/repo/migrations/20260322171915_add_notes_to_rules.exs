defmodule Maestro.Repo.Migrations.AddNotesToRules do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:rules) do
      add :notes, :text
    end
  end

  def down do
    alter table(:rules) do
      remove :notes
    end
  end
end
